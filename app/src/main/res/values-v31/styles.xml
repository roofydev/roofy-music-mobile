<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Starting theme for API 31+: system splash screen uses the adaptive icon automatically -->
    <style name="Theme.RoofyMusic.Starting" parent="Theme.RoofyMusic" />

    <!-- Widget Theme for Android 12+ with Dynamic Colors -->
    <style name="Theme.Widget.RoofyMusic" parent="@android:style/Theme.DeviceDefault.DayNight" />

    <style name="Theme.RoofyMusic.Transparent" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
